# Checksums are generated after freeze/bundle. This file is a contract stub until then.
# Run: python rewardlens/scripts/write_sha256sums.py --root <dir> --out <dir>/SHA256SUMS.txt
PLACEHOLDER  cloud_staging scripts exist; hashes written at packaging time
